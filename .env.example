# ===========================================
# ePortfolio - Environment Template
# ===========================================
# Copy to .env and set values. Never commit .env.

# ----- Environment -----
# Options: local | production
ENVIRONMENT=production

# ----- Django Core -----
SECRET_KEY="3p#ss^n1$!t^44@5f!s)@a%+ua#wydnh1&lxp0^u!28he6us"
# DEBUG and ALLOWED_HOSTS are set by ENVIRONMENT (local vs production).

# ----- Local overrides (optional) -----
# For local only: extra hosts (comma-separated)
# ALLOWED_HOSTS=localhost,127.0.0.1

# ----- Production: ALLOWED_HOSTS (required when ENVIRONMENT=production) -----
ALLOWED_HOSTS=shekhardhakal.com.np,www.shekhardhakal.com.np

# ----- Database -----
# Local: SQLite is used automatically (no env needed).
# Production: MySQL is used; set the following.

# MySQL (production)
DB_NAME=shekhard_hakal
DB_USER=shekhard_user
DB_PASSWORD=SpringWinter@2025
DB_HOST=localhost
DB_PORT=3306

# ----- Email (SMTP for both local and production) -----
# Sender address for all outgoing mail (contact/newsletter notifications).
EMAIL_FROM=noreply@shekhardhakal.com.np

# SMTP: SSL (port 465) or TLS (port 587). Set in .env for local and production.
EMAIL_HOST=s1308.sgp1.mysecurecloudhost.com
EMAIL_PORT=465
EMAIL_USE_SSL=True
EMAIL_USE_TLS=False
EMAIL_HOST_USER=noreply@shekhardhakal.com.np
EMAIL_HOST_PASSWORD=SpringWinter!
DEFAULT_FROM_EMAIL=noreply@shekhardhakal.com.np

# ----- Static & Media (production / cPanel) -----
# Override if not using default cPanel paths below.
# STATIC_ROOT=/home/shekhard/public_html/staticfiles
# MEDIA_ROOT=/home/shekhard/public_html/media
# STATIC_URL=/static/
# MEDIA_URL=/media/
# Optional: if cPanel serves static, use default Django storage instead of WhiteNoise:
# STATICFILES_STORAGE=django.contrib.staticfiles.storage.StaticFilesStorage

# ----- Optional -----
SITE_URL=https://shekhardhakal.com.np
